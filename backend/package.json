{
    "name": "@vatefs/backend",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "readmeFilename": "README.md",
    "scripts": {
        "build": "rm -rf dist/ && esbuild src/server.ts --bundle --platform=node --target=node22 --outfile=dist/server.js && cp -r public/ dist/ && cp package.json dist/ && cd dist/ && npm install --save ../../common && npm install --only=prod",
        "build:only": "esbuild src/server.ts --bundle --platform=node --target=node22 --outfile=dist/server.js",
        "start": "tsx --watch src/server.ts",
        "playback": "tsx src/playback.ts"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/js-yaml": "^4.0.9",
        "@types/node": "^24.10.0",
        "esbuild": "^0.25.12",
        "tsx": "^4.20.6",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "@vatefs/common": "file:../common",
        "express": "^5.1.0",
        "js-yaml": "^4.1.1",
        "moment": "^2.30.1",
        "morgan": "^1.10.1",
        "node-wav-player": "^1.0.0",
        "serve-static": "^2.2.0",
        "ws": "^8.18.3"
    }
}
