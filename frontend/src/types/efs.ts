// Re-export types from @vatefs/common for backwards compatibility
export type {
    StripType,
    FlightRules,
    WakeCategory,
    FlightStrip,
    Section,
    Bay,
    EfsConfig
} from "@vatefs/common"
